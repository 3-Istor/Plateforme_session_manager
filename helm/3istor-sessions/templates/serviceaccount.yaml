{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: "{{ include "3istor-sessions.serviceAccountName" . }}"
  labels:
    {{- include "3istor-sessions.labels" . | nindent 4 }}
{{- end }}
